<h1>Groups#show</h1>
<p>Find me in app/views/groups/show.html.erb</p>

<h4><%= @group.name %></h4>
<p>Users:</p>
<ul>
  <% @group.users.each do |user| %>
    <% if user.charges.first %>
    <li><%= user.username %> - <%= user.charges.first.category %></li>
  <% end %>
  <% end %>
</ul>

<%= render 'lay_charges_partial' %>
<h3>Snitch Someone</h3>

<%= render partial: "shared/scoreboard" %>

